/*
import express from 'express';
import controller from '../controllers/user';
import extractFirebaseInfo from '../middleware/extractFirebaseInfo';

const router = express.Router();

router.get('apiV1/user/validate', extractFirebaseInfo, controller.validate);
router.get('apiV1/user/:userID', controller.read);
router.post('apiV1/user/create', controller.create);
router.post('apiV1/user/login', controller.login);
router.get('apiV1/user/', controller.readAll);

export = router;*/
